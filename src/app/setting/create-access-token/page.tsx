import React from "react";
import CreateAccessToken from "@/components/setting/CreateAccessToken";

const page = () => {
  return (
    <div>
      <CreateAccessToken />
    </div>
  );
};

export default page;
